# ADR 0024: トポロジーに基づく選択操作

## Status

Accepted

## Decision

- Vertexは辺で接続された頂点、Edgeは端点を共有する辺、Faceは辺を共有する面を隣接要素とする。
- 選択の拡張・縮小・連結は同じ隣接グラフを利用する。
- Edge Loopは四角面内の反対辺をたどる。
- 選択操作は形状と履歴を変更せず、現在選択だけを更新する。

## Consequences

- 種別ごとに一貫した選択展開ができる。
- 三角面やN-gonを横断する高度なループ推定は行わない。
