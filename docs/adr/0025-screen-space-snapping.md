# ADR 0025: 画面距離による要素スナップ

## Status

Accepted

## Decision

- Vertex、Edge、Faceスナップを独立して切り替えられるようにする。
- Edgeは辺の中点、Faceは面の中心を候補点とする。
- ワールド距離ではなく、カメラ投影後の画面距離12pxを吸着閾値とする。
- 選択中の頂点を含む要素は候補から除外する。

## Consequences

- ズーム率やモデル単位に左右されにくい操作感になる。
- 現段階のEdge / Faceスナップ位置は代表点であり、辺上・面上の最近傍点は将来拡張とする。
